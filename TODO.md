# TODO:
- [ ] Remove the non-working --list feature
- [ ] Make it work with device names instead of the event path
- [ ] Somehow it doesn't work if the application receives mouse 5 + the key as a modifier, maybe modify it so it's press - select - press again
- [ ] Make it start at the first element of the list
- [ ] Make the program grab the mouse inputs, I mean rx/ry and scrolls, and don't let any other app read those
- [ ] Make the scroll/mouse option work, and add an option for both
- [ ] Make the color, size, and fonts configurable
- [ ] Make it able to execute commands
- [ ] Make it so you can press key combos with it, instead of just one